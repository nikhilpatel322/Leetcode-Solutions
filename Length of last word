class Solution {
    public int lengthOfLastWord(String s) {
        
        String[] wordArray=s.trim().split(" ");

        String a=wordArray[wordArray.length-1];
        return a.length();
         
    }
}
